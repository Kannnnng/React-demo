/**
 *
 * {{ properCase name }} actions
 *
 */

import { createAction } from 'redux-actions'
import {
  request,
} from './sources'

export const defaultAction = createAction('app/{{ properCase name }}/DEFAULT_ACTION', request)
