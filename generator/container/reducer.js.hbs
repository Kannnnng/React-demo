/**
 *
 * {{ properCase name }} reducer
 *
 */

import { fromJS } from 'immutable'
import { handleActions } from 'redux-actions'

const initialState = fromJS({})

export default handleActions({
  'APP/{{ constantCase name }}/DEFAULT_ACTION': {
    next(state) {
      return state
    },
    throw(state) {
      return state
    },
  },
}, initialState)
